language: node_js
cache:
  directories:
    - node_modules
install:
  - npm install -g firebase-tools
script:
  - npm run build
  - npm test

after_success:
  - firebase deploy --token %FIREBASE_TOKEN
